Original version of these scripts were designed on and for Windows, specifically Git Bash. 

They are proven to work there (yet adapted where necessary for Linux and theoretically for Mac (untested) )

Simply put in an executable directory viewable by path. I found it best to put them in the gitbash directory. 
E.g. C:\Program Files\Git\usr\bin. 